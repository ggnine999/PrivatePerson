import { marked, type Tokens } from 'marked';
import hljs from 'highlight.js';
const escape = (value:string) => value.replace(/[&<>"']/g, char => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[char]!));
export function renderMarkdown(source:string) { const renderer = new marked.Renderer(); renderer.html = ({ text }) => escape(text); renderer.code = ({ text, lang }: Tokens.Code) => { const language = lang && hljs.getLanguage(lang) ? lang : undefined; const highlighted = language ? hljs.highlight(text,{language}).value : hljs.highlightAuto(text).value; return `<pre><code class="hljs${language?` language-${escape(language)}`:''}">${highlighted}</code></pre>`; }; renderer.heading = ({tokens,depth}:Tokens.Heading) => { const text=renderer.parser.parseInline(tokens); const plain=tokens.map(token=>'text' in token?String(token.text):'').join(''); const id=plain.toLowerCase().replace(/[^\p{L}\p{N}]+/gu,'-').replace(/^-|-$/g,''); return `<h${depth} id="${escape(id)}">${text}</h${depth}>`; }; return marked.parse(source,{renderer,async:false}) as string; }
export function getHeadings(source:string){ return [...source.matchAll(/^##\s+(.+)$/gm)].map(match=>({text:match[1],id:match[1].toLowerCase().replace(/[^\p{L}\p{N}]+/gu,'-').replace(/^-|-$/g,'')})); }
