# 技术债与上线前检查清单

更新时间：2026-09-05
维护约定：解决一项就移入「已解决」；新增债务按优先级追加并标注位置与建议。

## 高优先（公开部署前必办）

- [ ] **壁纸/贴纸素材授权确认**：`public/images/saber-meadow-v2.avif`、`.webp` 源自 Wallpaper Engine 预览扩展生成，无再分发许可证（可编辑源图已移至 `assets/source/`）；`public/images/mascots/` 为用户提供的卡通图。公开部署前必须确认授权或替换。
- [ ] **CSP `script-src 'unsafe-inline'`**（`proxy.ts`）：React/Vinext 当前运行方式仍依赖内联脚本，削弱零知识保险库的 XSS 防线。正式部署前应验证框架 nonce/hash 支持并收紧。
- [ ] **生产环境变量**：`OWNER_LOGIN`/`OWNER_PASSWORD_HASH` 未配置时生产环境无人能登录（fail-closed，属预期）；上线前需生成正式哈希（`npm run auth:hash`）并配置。可选：`OWNER_TOTP_SECRET` 强制 2FA。
- [ ] **D1 生产绑定**：`wrangler.jsonc` 的 `database_id` 仍为占位符 `00000000-…`；部署前需创建真实 D1、替换绑定并应用四条迁移。

## 中优先（体验 / 正确性）

- [ ] **公开博客与保险库仍共享构建产物/origin**：当前已停用 Giscus 远程脚本并对私人路由使用更严格 CSP；若要恢复第三方评论，必须先把博客和保险库拆到不同 origin。
- [ ] **歌词逐字节奏为线性近似**：句级时间轴均分到字（`components/music-player.tsx`），与真实演唱存在偏差。要逐字精准需增强 LRC 或语音对齐数据源。
- [ ] **第三方公开功能可用性**：一言和网易云搜索/外链播放器依赖外部服务；均有失败降级，但外部接口、版权策略或嵌入规则变更时仍需维护。

## 低优先（优化 / 维护）

- [ ] **文章插画无现代格式**：`public/images/night-editorial.png` 单张约 2MB，建议补 AVIF/WebP 变体。
- [ ] **请求限速历史清理**：`request_rate_limits` 每个 scope/client 保留一行并在下次访问时重置，不包含敏感数据；若公网流量明显增长，可增加定时清理过期桶。
- [ ] **代码风格混杂**：本轮已格式化所有改动文件，少数未触及的旧文件仍为压缩单行风格；可在独立低风险变更中统一格式化并回归。
- [ ] **看板娘台词较少**：`components/kanban-musume.tsx` 的 `LINES` 数组仅 6 句，可按站点人设扩充。
- [ ] **Windows 下 Vite 文件监视偶发漏变更**：组件改动后 dev server 可能持续供应旧模块；重启 dev server 即可，属工具链已知问题。

## 已解决（存档备查）

- ~~保险库锁定后编辑表单仍保留解密明文~~ → 统一清理锁定、退出、保存、取消及对话框状态（2026-09-05）
- ~~编辑记录可直接解密且敏感输入明文显示~~ → 编辑前强制再次验证主密码，密码/Key/Secret 默认遮罩（2026-09-05）
- ~~Giscus 远程脚本与保险库共享同源权限~~ → 同源构建安全停用第三方评论脚本，并从 CSP 移除其域名（2026-09-05）
- ~~登录限速存在 SELECT/UPDATE 并发竞态且生产信任可伪造转发头~~ → D1 UPSERT 原子占位；生产只信任 Cloudflare 客户端地址头（2026-09-05）
- ~~网易云搜索无持久限速且缓存头被代理覆盖~~ → 新增 D1 原子限速表，公开 API 不再套用私人 no-store（2026-09-05）
- ~~切换网易云歌曲时本地音频继续播放~~ → 选择外链歌曲前显式暂停并同步播放器状态（2026-09-05）
- ~~保险库初始加载失败永久卡死~~ → 增加错误态和重试入口（2026-09-05）
- ~~过期会话行不清理~~ → 创建会话时批量清理过期行（2026-09-05）
- ~~生产登录页显示虚构演示凭据~~ → 仅非生产构建渲染（2026-09-05）
- ~~开发依赖含 critical/high/moderate 公告~~ → 升级 Cloudflare/Vitest 工具链并覆盖安全 esbuild，完整 `npm audit` 为 0（2026-09-05）
- ~~壁纸 PNG 源图随 public 发布~~ → 移至 `assets/source/saber-meadow-v2.png`（2026-09-05）
- ~~音频生成脚本非原子覆盖~~ → 改为临时文件 + rename + 重试（2026-09-05）
- ~~播放器音量初始值与 UI 不同步~~ → 挂载时同步（2026-09-05）
- ~~歌词 `duration` 未初始化导致进度条 max=0~~ → 挂载时按 `readyState` 补读（2026-09-05）
- ~~歌词面板分支在组件重写中遗漏~~ → 截图回归发现后补回（2026-09-05）
