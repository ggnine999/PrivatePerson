import type { Metadata } from 'next';
import { ExternalLink, GitBranch } from 'lucide-react';
import { projects } from '@/lib/content';
export const metadata:Metadata={title:'我的项目',description:'个人作品与实验项目档案。'};
export default function ProjectsPage(){return <main className="page shell"><header className="page-head"><span className="kicker">PROJECT LOG</span><h1>我的项目</h1><p>一些把想法变成可用工具的练习。当前条目均为虚构示例，方便以后直接替换。</p></header><div className="project-list">{projects.map(project=><article key={project.slug}><div className="project-mark">{project.mark}</div><div className="meta"><span>{project.category}</span><span>{project.status}</span>{project.featured&&<span>精选</span>}</div><h2>{project.name}</h2><p>{project.description}</p><div className="tags">{project.tech.map(item=><span key={item}>{item}</span>)}</div><div className="project-actions"><a className="button primary" href={project.website} target="_blank" rel="noopener noreferrer">查看项目 <ExternalLink/></a>{project.repository&&<a className="button ghost" href={project.repository} target="_blank" rel="noopener noreferrer"><GitBranch/> 代码仓库</a>}</div></article>)}</div></main>}
