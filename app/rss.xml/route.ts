import { articles } from '@/lib/content';
const xml=(value:string)=>value.replace(/[<>&'"]/g,char=>({'<':'&lt;','>':'&gt;','&':'&amp;',"'":'&apos;','"':'&quot;'}[char]!));
export async function GET(){const base=process.env.NEXT_PUBLIC_SITE_URL??'http://localhost:3000';const items=articles.map(article=>`<item><title>${xml(article.title)}</title><link>${base}/articles/${article.slug}</link><guid>${base}/articles/${article.slug}</guid><pubDate>${new Date(article.publishedAt).toUTCString()}</pubDate><description>${xml(article.description)}</description></item>`).join('');return new Response(`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>星屿手记</title><link>${base}</link><description>记录工程实践、创作札记与个人项目。</description><language>zh-CN</language>${items}</channel></rss>`,{headers:{'content-type':'application/rss+xml; charset=utf-8','cache-control':'public, max-age=3600'}})}
