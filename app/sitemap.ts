import type { MetadataRoute } from 'next';
import { articles } from '@/lib/content';
export default function sitemap():MetadataRoute.Sitemap{const base=process.env.NEXT_PUBLIC_SITE_URL??'http://localhost:3000';const pages:MetadataRoute.Sitemap=['','/articles','/archive','/projects','/music','/about'].map(path=>({url:`${base}${path}`,lastModified:new Date(),changeFrequency:path===''?'weekly':'monthly',priority:path===''?1:.7}));return pages.concat(articles.map(article=>({url:`${base}/articles/${article.slug}`,lastModified:new Date(article.updatedAt),changeFrequency:'monthly' as const,priority:.8})))}
